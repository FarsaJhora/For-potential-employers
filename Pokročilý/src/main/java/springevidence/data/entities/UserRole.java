package springevidence.data.entities;

/**
 * Enum reprezentující role uživatele v systému.
 * Obsahuje tři možné role: INSURED, POLICY_HOLDER a ADMIN.
 */
public enum UserRole {
    INSURED,
    POLICY_HOLDER,
    ADMIN, USER, 
}